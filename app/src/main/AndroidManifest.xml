<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.phillipsong.gittrending"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".TrendingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.FavoriteActivity"/>

        <activity android:name=".ui.activity.LanguagesActivity"/>

        <activity android:name=".ui.activity.AboutActivity"/>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="a8dbb91c6c25377d1cb8886e9939d5d015d59c3a" />
    </application>

</manifest>
